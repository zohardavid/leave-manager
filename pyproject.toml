[project]
name = "leave-manager"
version = "0.1.0"
description = "Military reserve unit leave management application"
requires-python = ">=3.11"
dependencies = [
    "pandas>=3.0.2",
        "streamlit>=1.56.0",
        ]
        
